<tr>
  <% Fund.column_names.each do |column| %>
    <td><%= fund.send(column) %></td>
  <% end %>
  <td><%= link_to t(:edit), edit_fund_path(fund) %></td>
  <td><%= link_to t(:destroy), fund, method: :delete, data: {confirm: t(:are_you_sure)} %></td>
</tr>